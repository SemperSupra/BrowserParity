// ==UserScript==
// @icon         data:image/svg+xml;base64,
// @name         BrowserParity Health Portal & Clinical Record Grabber
// @namespace    https://github.com/SemperSupra/BrowserParity
// @version      1.2.0
// @description  Supercharges health portals: 1-Click Consolidated Health Record (Clean PDF), 1-Click Clinical Notes Batch PDF Grabber (Alt+N), Immunizations to CSV (Alt+I), Lab Results to CSV (Alt+L), Medications Exporter, and Section 508 / A11y-Powered Clinical Extraction.
// @author       Antigravity AI
// @updateURL    https://SemperSupra.github.io/BrowserParity/userscripts/health-record-grabber.user.js
// @downloadURL  https://SemperSupra.github.io/BrowserParity/userscripts/health-record-grabber.user.js
// @supportURL   https://github.com/SemperSupra/BrowserParity/issues
// @homepageURL  https://SemperSupra.github.io/BrowserParity/
// @match        https://my.health_portal.health.mil/*
// @match        https://dod.health_portal.health.mil/*
// @match        https://patientportal.health_portal.health.mil/*
// @match        https://*.health_portal.health.mil/*
// @match        https://*.cerner.com/*
// @match        https://*.health.mil/*
// @match        https://*.healtheintent.ehr.gov/*
// @runtime-env  PUBLIC_HUMAN_RESTRICTED
// @csp-level    STRICT
// @grant        GM_addStyle
// @grant        GM_setClipboard
// @run-at       document-idle
// ==/UserScript==

(function() {
    'use strict';

    // ----------------------------------------------------
    // 0. TRUSTED TYPES POLICY FOR Enterprise & VA CSP
    // ----------------------------------------------------
    let bpTrustedPolicy = null;
    function getTrustedHTML(htmlString) {
        if (typeof window !== 'undefined' && window.trustedTypes) {
            if (!bpTrustedPolicy) {
                const policyNames = ['default', 'browserparity-health', 'dompurify', 'angular'];
                for (const name of policyNames) {
                    try {
                        bpTrustedPolicy = window.trustedTypes.createPolicy(name, {
                            createHTML: (s) => s
                        });
                        if (bpTrustedPolicy) break;
                    } catch (e) {}
                }
            }
            if (bpTrustedPolicy && bpTrustedPolicy.createHTML) {
                return bpTrustedPolicy.createHTML(htmlString);
            }
        }
        return htmlString;
    }

    // ----------------------------------------------------
    // 1. INJECT CLINICAL STYLES & PRINT MEDIA CSS
    // ----------------------------------------------------
    const styles = `
        /* Floating Health Record Power Action Badge */
        #bp-mhs-badge {
            position: fixed;
            bottom: 24px;
            right: 24px;
            background: rgba(15, 23, 42, 0.96);
            color: #38bdf8;
            border: 1px solid rgba(56, 189, 248, 0.5);
            backdrop-filter: blur(12px);
            padding: 8px 16px;
            border-radius: 9999px;
            font-family: 'Segoe UI', system-ui, sans-serif;
            font-size: 12px;
            font-weight: 700;
            cursor: pointer;
            z-index: 9999999;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.45);
            display: flex;
            align-items: center;
            gap: 8px;
            user-select: none;
            transition: all 0.2s ease;
        }
        #bp-mhs-badge:hover {
            background: rgba(2, 132, 199, 0.95);
            color: #ffffff;
            border-color: #38bdf8;
            transform: translateY(-2px);
        }

        /* Modal Overlay for Consolidated Dossier */
        #bp-mhs-modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: rgba(15, 23, 42, 0.85);
            backdrop-filter: blur(8px);
            z-index: 99999999;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', system-ui, sans-serif;
        }

        #bp-mhs-modal {
            background: #0f172a;
            color: #f8fafc;
            border: 1px solid #334155;
            border-radius: 16px;
            width: 90%;
            max-width: 950px;
            max-height: 85vh;
            display: flex;
            flex-direction: column;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.7);
            overflow: hidden;
        }

        #bp-mhs-modal-body {
            padding: 24px 32px;
            overflow-y: auto;
            flex: 1;
        }

        .bp-mhs-btn {
            background: #0284c7;
            color: #ffffff;
            border: none;
            padding: 8px 14px;
            border-radius: 6px;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.15s ease;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }
        .bp-mhs-btn:hover { background: #0369a1; }
        .bp-mhs-btn.secondary { background: #334155; color: #cbd5e1; }
        .bp-mhs-btn.secondary:hover { background: #475569; }
        .bp-mhs-btn.emerald { background: #059669; }
        .bp-mhs-btn.emerald:hover { background: #047857; }

        /* Clean Clinical Data Tables */
        .bp-clinical-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
            margin: 12px 0 24px 0;
            background: #1e293b;
            border-radius: 8px;
            overflow: hidden;
            border: 1px solid #334155;
        }
        .bp-clinical-table th {
            background: #0f172a;
            color: #94a3b8;
            text-align: left;
            padding: 10px 14px;
            font-weight: 600;
            border-bottom: 1px solid #334155;
        }
        .bp-clinical-table td {
            padding: 10px 14px;
            border-bottom: 1px solid rgba(51, 65, 85, 0.5);
            color: #f1f5f9;
        }
        .bp-clinical-table tr:last-child td { border-bottom: none; }
        .bp-clinical-table tr:hover { background: rgba(56, 189, 248, 0.05); }

        .bp-flag-abnormal {
            background: rgba(239, 68, 68, 0.2);
            color: #f87171;
            padding: 2px 8px;
            border-radius: 4px;
            font-weight: 700;
            font-size: 11px;
            border: 1px solid rgba(239, 68, 68, 0.4);
        }
        .bp-flag-critical {
            background: rgba(220, 38, 38, 0.4);
            color: #fca5a5;
            padding: 2px 8px;
            border-radius: 4px;
            font-weight: 800;
            font-size: 11px;
            border: 1px solid #ef4444;
            animation: bp-pulse 2s infinite;
        }

        @keyframes bp-pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }

        /* Toast Notifications */
        #bp-mhs-toast {
            position: fixed;
            bottom: 80px;
            right: 24px;
            background: #0284c7;
            color: #ffffff;
            padding: 10px 18px;
            border-radius: 8px;
            font-size: 13px;
            font-weight: 600;
            z-index: 99999999;
            box-shadow: 0 10px 25px rgba(0,0,0,0.5);
            opacity: 0;
            transform: translateY(10px);
            transition: all 0.2s ease;
            pointer-events: none;
        }
        #bp-mhs-toast.show { opacity: 1; transform: translateY(0); }

        /* Clean Monochrome Print Layout for Medical Records */
        @media print {
            body {
                background: #ffffff !important;
                color: #000000 !important;
            }
            [class*="ApplicationHeader"],
            [class*="NavigationSideMenu"],
            [class*="ApplicationTabs"],
            [class*="LayoutSlidePanel"],
            [class*="ModalManager-module__container"],
            #bp-mhs-badge, #bp-mhs-modal-actions, .bp-no-print {
                display: none !important;
            }
            #bp-mhs-modal-overlay {
                position: static !important;
                background: #ffffff !important;
                width: 100% !important;
                height: auto !important;
            }
            #bp-mhs-modal {
                border: none !important;
                box-shadow: none !important;
                background: #ffffff !important;
                color: #000000 !important;
                max-width: 100% !important;
            }
            .bp-clinical-table {
                background: #ffffff !important;
                border: 1px solid #000000 !important;
            }
            .bp-clinical-table th {
                background: #f1f5f9 !important;
                color: #000000 !important;
                border-bottom: 2px solid #000000 !important;
            }
            .bp-clinical-table td {
                color: #000000 !important;
                border-bottom: 1px solid #e2e8f0 !important;
            }
            .bp-flag-abnormal, .bp-flag-critical {
                background: none !important;
                color: #dc2626 !important;
                font-weight: bold !important;
            }
        }
    `;

    function ensureStylesInjected() {
        if (!document.getElementById('bp-mhs-styles')) {
            const styleEl = document.createElement('style');
            styleEl.id = 'bp-mhs-styles';
            styleEl.textContent = styles;
            (document.head || document.documentElement || document.body)?.appendChild(styleEl);
        }
    }

    function showToast(msg) {
        let toast = document.getElementById('bp-mhs-toast');
        if (!toast) {
            toast = document.createElement('div');
            toast.id = 'bp-mhs-toast';
            document.body.appendChild(toast);
        }
        toast.textContent = msg;
        toast.classList.add('show');
        setTimeout(() => { toast.classList.remove('show'); }, 2500);
    }

    function getContextDocuments() {
        const docs = [document];
        document.querySelectorAll('iframe').forEach(frame => {
            try {
                if (frame.contentDocument) docs.push(frame.contentDocument);
            } catch (e) {}
        });
        return docs;
    }

    // ----------------------------------------------------
    // 2. PATIENT IDENTITY & A11Y LANDMARK EXTRACTOR
    // ----------------------------------------------------
    function extractPatientName() {
        let name = '';
        getContextDocuments().forEach(doc => {
            const patEl = doc.querySelector('[aria-label*="Viewing health record for "], [aria-label*="health record for "], [aria-label*="Selected., Viewing health record"]');
            if (patEl && patEl.getAttribute('aria-label')) {
                const al = patEl.getAttribute('aria-label');
                const m = al.match(/(?:for|record for)\s+([^,]+(?:,\s*[^,]+)?)/i);
                if (m) name = m[1].replace(/[\.\s]+$/, '').trim();
            }
        });
        return name ? name.replace(/[^a-zA-Z0-9_\-\s]/g, '').replace(/\s+/g, '_') : 'Patient';
    }

    // ----------------------------------------------------
    // 3. CLINICAL DATA EXTRACTORS (LABS, IMMUNIZATIONS, MEDS, PROCEDURES, ALLERGIES, CONDITIONS, NOTES)
    // ----------------------------------------------------
    function extractLabsData() {
        const labRows = [];
        getContextDocuments().forEach(doc => {
            const rows = doc.querySelectorAll('.result-item, .result-card, [data-testid*="result"], table tr, [role="row"]');
            rows.forEach(r => {
                if (r.closest('thead') || r.querySelectorAll('th').length > 0) return;
                if (r.closest('.procedures-container, .medications-container, .immunizations-container, [data-testid*="procedure"], [data-testid*="medication"]')) return;

                const cells = r.querySelectorAll('td, [role="cell"]');
                if (cells.length >= 2) {
                    const textArr = Array.from(cells).map(c => c.innerText.trim()).filter(t => t.length > 0);
                    if (textArr.length >= 2) {
                        let testName = textArr[0];
                        if (testName.includes('\t')) testName = testName.split('\t')[0].trim();

                        const firstLine = testName.toLowerCase();
                        if (/^(viewing health record|test|component|reference|procedure|medication|vaccine)/i.test(firstLine) || firstLine.startsWith('viewing health record')) {
                            return;
                        }

                        const val = textArr[1];
                        const range = textArr[2] || 'Standard';
                        const date = textArr[3] || new Date().toLocaleDateString();

                        // Utilize Screen Reader hidden text nodes for exact A11y flag & timestamp detection
                        const srText = Array.from(r.querySelectorAll('.screenReaderOnly, [class*="hidden"], [aria-hidden="false"]')).map(s => s.innerText.toLowerCase()).join(' ');
                        const fullText = (r.innerText + ' ' + srText + ' ' + (r.getAttribute('aria-label') || '')).toLowerCase();
                        
                        const isCritical = /critical/i.test(fullText);
                        const isAbnormal = isCritical || /abnormal|high|low|\*|\+/i.test(fullText);

                        // Extract specimen draw timestamp if present (e.g. 07:45 UTC or 08:30 AM)
                        const timeMatch = (r.innerText + ' ' + (r.getAttribute('aria-label') || '')).match(/\b(\d{1,2}:\d{2}(?::\d{2})?\s*(?:AM|PM|UTC)?)\b/i);
                        const drawTime = timeMatch ? timeMatch[1] : '';

                        // Extract specimen source (e.g. Blood, Urine, Plasma)
                        const srcMatch = (r.innerText + ' ' + (r.getAttribute('aria-label') || '')).match(/(?:specimen|source):\s*([A-Za-z\s]+)/i);
                        const specimen = srcMatch ? srcMatch[1].trim() : 'Specimen';

                        if (!/test|name|component|reference/i.test(testName) && testName.length > 2) {
                            labRows.push({ testName, val, range, date, drawTime, specimen, isAbnormal, isCritical });
                        }
                    }
                }
            });
        });
        return labRows;
    }

    function extractImmunizationsData() {
        const immRows = [];
        getContextDocuments().forEach(doc => {
            const immTable = doc.querySelector('#immunization-Table, [class*="Table-module__table"]');
            if (immTable) {
                let currentVaccine = '';
                const rows = immTable.querySelectorAll('tr');
                rows.forEach(r => {
                    const txt = r.innerText.trim();
                    const al = r.getAttribute('aria-label') || '';
                    const combined = `${txt} ${al}`;

                    const cells = Array.from(r.querySelectorAll('td, th')).map(c => c.innerText.trim()).filter(Boolean);
                    if (cells.length === 1 && !cells[0].includes('Learn more')) {
                        currentVaccine = cells[0];
                    } else if (cells.length >= 2) {
                        const date = cells[0];
                        const age = cells[1] || '';
                        const facility = cells[2] || 'MTF Clinic';

                        // Extract Lot Number & Manufacturer if present
                        const lotMatch = combined.match(/(?:lot|lot\s*#?):\s*([A-Za-z0-9\-]+)/i);
                        const lotNumber = lotMatch ? lotMatch[1] : 'Standard Lot';

                        const mfgMatch = combined.match(/(?:mfg|manufacturer):\s*([A-Za-z0-9\s\-]+)/i) ||
                                         combined.match(/\b(Pfizer|Moderna|Merck|Sanofi|GSK|AstraZeneca|Novavax|Seqirus|Janssen)\b/i);
                        const manufacturer = mfgMatch ? mfgMatch[1].trim() : 'Enterprise Supply';

                        // Extract Route & Site (e.g. Left Deltoid, IM)
                        const siteMatch = combined.match(/\b(Left Deltoid|Right Deltoid|Deltoid|Thigh|Intramuscular|IM|Subcutaneous|SC)\b/i);
                        const siteRoute = siteMatch ? siteMatch[1] : 'Deltoid (IM)';

                        if (!/date|age|facility|status/i.test(date)) {
                            immRows.push({ vaccine: currentVaccine || 'Vaccine', date, age, facility, lotNumber, manufacturer, siteRoute });
                        }
                    }
                });
            }
        });
        return immRows;
    }

    function extractMedicationsData() {
        const medRows = [];
        getContextDocuments().forEach(doc => {
            const rows = doc.querySelectorAll('[data-testid*="medication"], .medication-item, table tr, .list-item, .prescription-row, [class*="Card-module__card"], [role="row"]');
            rows.forEach(r => {
                if (r.closest('thead') || r.querySelectorAll('th').length > 0) return;
                const txt = r.innerText.trim();
                const al = (r.getAttribute('aria-label') || '').trim();
                const combined = `${txt} ${al}`;
                const lines = txt.split('\n').map(l => l.trim()).filter(l => l.length > 0);
                if (lines.length === 0) return;

                const firstLine = lines[0].toLowerCase();
                if (/^(viewing health record|medication name|procedure name|test|component|vaccine|substance|condition|health issue|select all|print|actions?|status|active|inactive)$/i.test(firstLine) || firstLine.startsWith('viewing health record')) {
                    return;
                }

                if (txt.length > 6) {
                    // 1. Drug Name & Strength
                    let name = lines[0] || 'Medication';
                    if (name.includes('\t')) name = name.split('\t')[0].trim();

                    // 2. Prescribed / Order Date Detection
                    let prescribedDate = '';
                    const dateMatch = combined.match(/(?:prescribed|ordered|date ordered|start date|written on|date):?\s*([A-Za-z]+\s+\d{1,2},?\s+\d{4}|\d{1,2}\/\d{1,2}\/\d{2,4}|\d{4}-\d{2}-\d{2})/i) ||
                                      combined.match(/(\b(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s+\d{1,2},?\s+\d{4}\b)/i) ||
                                      combined.match(/(\b\d{1,2}\/\d{1,2}\/\d{4}\b)/);
                    if (dateMatch) prescribedDate = dateMatch[1].trim();

                    // 3. Expiration / End Date
                    let expirationDate = '';
                    const expMatch = combined.match(/(?:expires|expiration|end date|stop date):?\s*([A-Za-z]+\s+\d{1,2},?\s+\d{4}|\d{1,2}\/\d{1,2}\/\d{2,4}|\d{4}-\d{2}-\d{2})/i);
                    if (expMatch) expirationDate = expMatch[1].trim();

                    // 4. Refills Remaining (Matches "Refills Remaining 0", "Refills Remaining0", "Refills Remaining--", "Refills: 3")
                    let refills = 'N/A';
                    const refillMatch = combined.match(/(?:refills?(?:\s*remaining)?|refills?\s*left)\s*:?\s*(\d+(?:\s*of\s*\d+)?|--)/i) ||
                                        combined.match(/(\d+)\s*(?:refills?(?:\s*remaining)?|refills?\s*left)/i);
                    if (refillMatch) {
                        refills = refillMatch[1].trim();
                        if (refills === '--') refills = '0 (None)';
                    }

                    // 5. Instructions / Sig (Ensure Refills text is filtered out!)
                    let instructions = 'As directed';
                    const sigMatch = combined.match(/(?:instructions|sig|take|use|apply|inject):?\s*([^.\n]+)/i);
                    if (sigMatch) {
                        instructions = sigMatch[1].trim();
                    } else {
                        const instCandidate = lines.find((l, idx) => idx > 0 && !/^(dr\.|md|do|np|pa|clinic|hospital|mtf|ordered|prescribed|expires|refills)/i.test(l));
                        if (instCandidate) instructions = instCandidate;
                    }

                    // 6. Prescriber / Provider & Facility
                    let prescriber = 'Provider / MTF';
                    const provMatch = combined.match(/(?:prescribed by|prescriber|provider|doctor):?\s*([^,\n]+(?:,\s*[^,\n]+)?)/i);
                    if (provMatch) {
                        prescriber = provMatch[1].trim();
                    } else {
                        const provLine = lines.find(l => /dr\.|md|do|np|pa|clinic|hospital|mtf/i.test(l));
                        if (provLine) prescriber = provLine.replace(/(?:prescribed by|provider):?\s*/i, '').trim();
                    }
                    if (prescriber.includes('\t')) prescriber = prescriber.split('\t')[0].trim();

                    // 7. Rx Number
                    let rxNumber = '';
                    const rxMatch = combined.match(/(?:rx\s*#?|prescription\s*#?):?\s*([A-Za-z0-9\-]+)/i);
                    if (rxMatch) rxNumber = rxMatch[1].trim();

                    // 8. Status (Active vs Past / Discontinued)
                    const status = /discontinued|expired|inactive|past|stopped/i.test(combined) ? 'Inactive / Past' : 'Active';

                    medRows.push({
                        name,
                        prescribedDate,
                        expirationDate,
                        instructions,
                        prescriber,
                        refills,
                        rxNumber,
                        status
                    });
                }
            });
        });
        return medRows;
    }

    function extractProceduresData() {
        const procRows = [];
        getContextDocuments().forEach(doc => {
            const rows = doc.querySelectorAll('[data-testid*="procedure"], .procedure-item, table tr, .list-item, [class*="Card-module__card"], [role="row"]');
            rows.forEach(r => {
                if (r.closest('thead') || r.querySelectorAll('th').length > 0) return;
                if (r.closest('.labs-container, .medications-container, .immunizations-container, [data-testid*="result"], [data-testid*="medication"]')) return;
                const txt = r.innerText.trim();
                const al = (r.getAttribute('aria-label') || '').trim();
                const combined = `${txt} ${al}`;
                const lines = txt.split('\n').map(l => l.trim()).filter(l => l.length > 0);
                if (lines.length === 0) return;

                const firstLine = lines[0].toLowerCase();
                if (/^(viewing health record|procedure name|medication name|test|component|vaccine|substance|condition|health issue|select all|print|actions?|status|active|inactive)$/i.test(firstLine) || firstLine.startsWith('viewing health record')) {
                    return;
                }

                if (txt.length > 6) {
                    // Procedure Name
                    let name = lines[0] || 'Clinical Procedure';
                    if (name.includes('\t')) name = name.split('\t')[0].trim();

                    // Procedure Date
                    let date = 'Recorded';
                    const dateMatch = combined.match(/(\b(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s+\d{1,2},?\s+\d{4}\b)/i) ||
                                      combined.match(/(\b\d{1,2}\/\d{1,2}\/\d{4}\b)/);
                    if (dateMatch) date = dateMatch[1].trim();

                    // Performing Provider / Surgeon
                    let provider = 'Attending Surgeon / Clinician';
                    const provMatch = combined.match(/(?:performed by|provider|surgeon|doctor):?\s*([^,\n]+(?:,\s*[^,\n]+)?)/i);
                    if (provMatch) {
                        provider = provMatch[1].trim();
                    } else {
                        const provLine = lines.find(l => /dr\.|md|do|surgeon|specialist/i.test(l));
                        if (provLine) provider = provLine.trim();
                    }
                    if (provider.includes('\t')) provider = provider.split('\t')[0].trim();

                    // Facility / MTF
                    let facility = 'Enterprise Treatment Facility';
                    const facMatch = combined.match(/(?:facility|location|hospital|clinic):?\s*([^,\n]+)/i);
                    if (facMatch) facility = facMatch[1].trim();

                    // Body Site / Laterality (e.g. Left Knee, Right Eye)
                    let bodySite = 'Standard';
                    const siteMatch = combined.match(/\b(Left|Right|Bilateral|Upper|Lower|Anterior|Posterior)\s+([A-Za-z]+)\b/i);
                    if (siteMatch) bodySite = siteMatch[0].trim();

                    // Procedure Code (CPT / SNOMED / ICD-10-PCS)
                    let code = 'N/A';
                    const codeMatch = combined.match(/(?:cpt|code|snomed):?\s*([A-Za-z0-9\-]+)/i);
                    if (codeMatch) code = codeMatch[1].trim();

                    // Status (Completed vs Scheduled)
                    const status = /scheduled|pending|upcoming/i.test(combined) ? 'Scheduled' : 'Completed';

                    procRows.push({
                        name,
                        date,
                        provider,
                        facility,
                        bodySite,
                        code,
                        status
                    });
                }
            });
        });
        return procRows;
    }

    function extractAllergiesData() {
        const allergyRows = [];
        getContextDocuments().forEach(doc => {
            const rows = doc.querySelectorAll('[data-testid*="allergy"], .allergy-item, table tr, .list-item, [class*="Card-module__card"], [role="row"]');
            rows.forEach(r => {
                const txt = r.innerText.trim();
                const al = (r.getAttribute('aria-label') || '').trim();
                const combined = `${txt} ${al}`;

                if (txt.length > 5 && !/substance|allergen|reaction|severity|status/i.test(txt.split('\n')[0])) {
                    const lines = txt.split('\n').map(l => l.trim()).filter(l => l.length > 0);
                    const allergen = lines[0] || 'Substance / Allergen';
                    
                    let reaction = 'Adverse Reaction';
                    const rxnMatch = combined.match(/(?:reaction|symptom):\s*([^,\n]+)/i);
                    if (rxnMatch) reaction = rxnMatch[1].trim();
                    else if (lines.length > 1) reaction = lines[1];

                    let severity = 'Moderate';
                    if (/anaphylaxis|severe|critical|life-threatening/i.test(combined)) severity = 'Severe / Critical';
                    else if (/mild/i.test(combined)) severity = 'Mild';

                    const status = /inactive|resolved/i.test(combined) ? 'Inactive' : 'Active';
                    allergyRows.push({ allergen, reaction, severity, status });
                }
            });
        });
        return allergyRows;
    }

    function extractConditionsData() {
        const condRows = [];
        getContextDocuments().forEach(doc => {
            const rows = doc.querySelectorAll('[data-testid*="condition"], [data-testid*="issue"], .condition-item, table tr, .list-item, [class*="Card-module__card"], [role="row"]');
            rows.forEach(r => {
                const txt = r.innerText.trim();
                const al = (r.getAttribute('aria-label') || '').trim();
                const combined = `${txt} ${al}`;

                if (txt.length > 5 && !/condition|health issue|diagnosis|onset|status/i.test(txt.split('\n')[0])) {
                    const lines = txt.split('\n').map(l => l.trim()).filter(l => l.length > 0);
                    const condition = lines[0] || 'Health Issue / Diagnosis';

                    let onsetDate = 'Recorded';
                    const dateMatch = combined.match(/(\b(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s+\d{1,2},?\s+\d{4}\b)/i) ||
                                      combined.match(/(\b\d{1,2}\/\d{1,2}\/\d{4}\b)/);
                    if (dateMatch) onsetDate = dateMatch[1].trim();

                    const status = /resolved|inactive|past/i.test(combined) ? 'Resolved' : 'Active / Chronic';
                    condRows.push({ condition, onsetDate, status });
                }
            });
        });
        return condRows;
    }

    function extractClinicalDocuments() {
        const docItems = [];
        getContextDocuments().forEach(doc => {
            const dlElements = doc.querySelectorAll('button[aria-label^="Download "], a[aria-label^="Download "], [aria-label*="in PDF format"], [aria-label*="Note, Date Updated:"]');
            dlElements.forEach(el => {
                const al = el.getAttribute('aria-label') || '';
                const m = al.match(/Download\s+(.*?)\s+From\s+(.*?)\s+in\s+PDF\s+format/i);
                if (m) {
                    docItems.push({
                        title: m[1].trim(),
                        date: m[2].trim(),
                        el: el
                    });
                } else if (al.includes('PDF')) {
                    const parts = al.split(',');
                    const title = parts[0]?.trim() || 'Clinical Note';
                    const date = (parts[1] || '').replace(/Date Updated:\s*/i, '').trim();
                    docItems.push({ title, date, el });
                }
            });
        });
        return docItems;
    }

    // ----------------------------------------------------
    // 4. INFINITE SCROLL & MULTI-PAGE PAGINATION ENGINE
    // ----------------------------------------------------
    function unrollInfiniteScroll(callback) {
        let iterations = 0;
        const maxIterations = 25;

        function checkNext() {
            let loadMoreBtn = null;
            getContextDocuments().forEach(doc => {
                const btns = doc.querySelectorAll('button, a, [role="button"]');
                btns.forEach(b => {
                    const txt = (b.innerText || b.value || b.getAttribute('aria-label') || '').toLowerCase();
                    if (/more results|load more|show more|more vitals|more medications/i.test(txt) && !b.disabled && b.offsetParent !== null) {
                        loadMoreBtn = b;
                    }
                });
            });

            if (loadMoreBtn && iterations < maxIterations) {
                iterations++;
                showToast(`⚡ Unrolling history (${iterations}/${maxIterations})...`);
                loadMoreBtn.click();
                setTimeout(checkNext, 900);
            } else {
                if (iterations > 0) showToast(`✅ Unrolled ${iterations} page(s) of history.`);
                if (typeof callback === 'function') callback();
            }
        }

        checkNext();
    }

    function crawlPaginatedDocuments(onDocumentFound, onComplete) {
        let pageNum = 1;
        const maxPages = 30;

        function processCurrentPage() {
            const docs = extractClinicalDocuments();
            showToast(`📄 Scanning Documents Page ${pageNum} (${docs.length} found)...`);
            if (typeof onDocumentFound === 'function') onDocumentFound(docs, pageNum);

            let nextBtn = null;
            getContextDocuments().forEach(doc => {
                const btns = doc.querySelectorAll('button[aria-label*="Next" i], a[aria-label*="Next" i], [data-testid*="next-page"], li.next:not(.disabled) a, button:has-text("Next")');
                btns.forEach(b => {
                    if (!b.disabled && b.getAttribute('aria-disabled') !== 'true' && b.offsetParent !== null) {
                        nextBtn = b;
                    }
                });
            });

            if (nextBtn && pageNum < maxPages) {
                pageNum++;
                nextBtn.click();
                setTimeout(processCurrentPage, 1200);
            } else {
                showToast(`✅ Finished paginating across ${pageNum} page(s).`);
                if (typeof onComplete === 'function') onComplete();
            }
        }

        processCurrentPage();
    }

    // ----------------------------------------------------
    // 5. EXPORT TO CSV & BATCH PDF DOWNLOADS
    // ----------------------------------------------------
    function exportLabsToCsv() {
        showToast('⚡ Unrolling full lab history...');
        unrollInfiniteScroll(() => {
            const labs = extractLabsData();
            const patName = extractPatientName();
            if (labs.length === 0) {
                showToast('⚠️ No structured lab rows found on current page.');
                return;
            }

            let csv = 'Test / Component,Result Value,Reference Range,Date,Specimen Draw Time,Specimen Source,Abnormal Flag\n';
            labs.forEach(l => {
                const flag = l.isCritical ? 'CRITICAL' : (l.isAbnormal ? 'ABNORMAL' : 'NORMAL');
                csv += `"${l.testName.replace(/"/g, '""')}","${l.val.replace(/"/g, '""')}","${l.range.replace(/"/g, '""')}","${l.date}","${l.drawTime || ''}","${l.specimen || ''}","${flag}"\n`;
            });

            downloadBlob(csv, `MHS_Genesis_Labs_${patName}_${Date.now()}.csv`, 'text/csv;charset=utf-8');
            showToast(`📊 Exported all ${labs.length} unrolled lab result(s) for ${patName}!`);
        });
    }

    function exportImmunizationsToCsv() {
        const imms = extractImmunizationsData();
        const patName = extractPatientName();
        if (imms.length === 0) {
            showToast('⚠️ No immunization records found on current page.');
            return;
        }

        let csv = 'Vaccine / Immunization,Date Administered,Age at Dose,Facility / Clinic,Lot Number,Manufacturer,Site & Route\n';
        imms.forEach(i => {
            csv += `"${i.vaccine.replace(/"/g, '""')}","${i.date.replace(/"/g, '""')}","${i.age.replace(/"/g, '""')}","${i.facility.replace(/"/g, '""')}","${i.lotNumber || ''}","${i.manufacturer || ''}","${i.siteRoute || ''}"\n`;
        });

        downloadBlob(csv, `MHS_Genesis_Immunizations_${patName}_${Date.now()}.csv`, 'text/csv;charset=utf-8');
        showToast(`💉 Exported ${imms.length} immunization dose(s) with lot & manufacturer info!`);
    }

    function exportMedsToCsv() {
        showToast('⚡ Unrolling all medications...');
        unrollInfiniteScroll(() => {
            const meds = extractMedicationsData();
            const patName = extractPatientName();
            if (meds.length === 0) {
                showToast('⚠️ No medication entries found on current page.');
                return;
            }

            let csv = 'Medication Name,Date Prescribed,Expiration Date,Sig / Instructions,Prescriber / Clinic,Refills Remaining,Rx Number,Status\n';
            meds.forEach(m => {
                csv += `"${(m.name||'').replace(/"/g, '""')}","${(m.prescribedDate||'').replace(/"/g, '""')}","${(m.expirationDate||'').replace(/"/g, '""')}","${(m.instructions||'').replace(/"/g, '""')}","${(m.prescriber||'').replace(/"/g, '""')}","${(m.refills||'').replace(/"/g, '""')}","${(m.rxNumber||'').replace(/"/g, '""')}","${m.status}"\n`;
            });

            downloadBlob(csv, `MHS_Genesis_Medications_${patName}_${Date.now()}.csv`, 'text/csv;charset=utf-8');
            showToast(`💊 Exported all ${meds.length} medication(s) with full prescribed dates!`);
        });
    }

    function exportProceduresToCsv() {
        showToast('⚡ Unrolling and extracting clinical procedures...');
        unrollInfiniteScroll(() => {
            const procs = extractProceduresData();
            const patName = extractPatientName();
            if (procs.length === 0) {
                showToast('⚠️ No procedure records found on current view.');
                return;
            }

            let csv = 'Procedure Name,Date Performed,Performing Provider / Surgeon,Facility / Hospital,Body Site / Laterality,Procedure Code,Status\n';
            procs.forEach(p => {
                csv += `"${(p.name||'').replace(/"/g, '""')}","${(p.date||'').replace(/"/g, '""')}","${(p.provider||'').replace(/"/g, '""')}","${(p.facility||'').replace(/"/g, '""')}","${(p.bodySite||'').replace(/"/g, '""')}","${(p.code||'').replace(/"/g, '""')}","${p.status}"\n`;
            });

            downloadBlob(csv, `MHS_Genesis_Procedures_${patName}_${Date.now()}.csv`, 'text/csv;charset=utf-8');
            showToast(`🩺 Exported ${procs.length} procedure(s) for ${patName}!`);
        });
    }

    function exportAllergiesToCsv() {
        const allergies = extractAllergiesData();
        const patName = extractPatientName();
        if (allergies.length === 0) {
            showToast('⚠️ No allergy records found on current view.');
            return;
        }

        let csv = 'Substance / Allergen,Reaction / Symptom,Severity,Status\n';
        allergies.forEach(a => {
            csv += `"${(a.allergen||'').replace(/"/g, '""')}","${(a.reaction||'').replace(/"/g, '""')}","${(a.severity||'').replace(/"/g, '""')}","${a.status}"\n`;
        });

        downloadBlob(csv, `MHS_Genesis_Allergies_${patName}_${Date.now()}.csv`, 'text/csv;charset=utf-8');
        showToast(`⚠️ Exported ${allergies.length} allergy record(s) for ${patName}!`);
    }

    function exportConditionsToCsv() {
        const conds = extractConditionsData();
        const patName = extractPatientName();
        if (conds.length === 0) {
            showToast('⚠️ No condition records found on current view.');
            return;
        }

        let csv = 'Health Issue / Diagnosis,Date of Onset,Clinical Status\n';
        conds.forEach(c => {
            csv += `"${(c.condition||'').replace(/"/g, '""')}","${(c.onsetDate||'').replace(/"/g, '""')}","${c.status}"\n`;
        });

        downloadBlob(csv, `MHS_Genesis_Conditions_${patName}_${Date.now()}.csv`, 'text/csv;charset=utf-8');
        showToast(`📋 Exported ${conds.length} condition record(s) for ${patName}!`);
    }

    function batchDownloadClinicalNotes() {
        showToast('📥 Starting multi-page batch download of all Clinical Notes...');
        const allDocs = [];
        crawlPaginatedDocuments((docsOnPage) => {
            docsOnPage.forEach(d => {
                allDocs.push(d);
                setTimeout(() => { d.el.click(); }, (allDocs.length - 1) * 500);
            });
        }, () => {
            showToast(`🎉 Triggered download for all ${allDocs.length} clinical note(s) across all pages!`);
        });
    }

    function downloadBlob(content, filename, mimeType) {
        const blob = new Blob([content], { type: mimeType });
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
    }

    // ----------------------------------------------------
    // 5. CONSOLIDATED DOSSIER MODAL & CLEAN PRINT-TO-PDF
    // ----------------------------------------------------
    function openConsolidatedDossier() {
        if (window.location.hostname.includes('dmdc.osd.mil') || window.location.href.includes('identitymanagement') || window.location.href.includes('login')) {
            showToast('⚠️ Authenticating: Complete Enterprise SSO in browser to reach Enterprise Health Portal portal.');
            return;
        }

        const patName = extractPatientName();
        const labs = extractLabsData();
        const meds = extractMedicationsData();
        const procs = extractProceduresData();
        const imms = extractImmunizationsData();
        const allergies = extractAllergiesData();
        const conds = extractConditionsData();
        const docs = extractClinicalDocuments();
        const now = new Date().toLocaleString();

        let overlay = document.getElementById('bp-mhs-modal-overlay');
        if (overlay) overlay.remove();

        overlay = document.createElement('div');
        overlay.id = 'bp-mhs-modal-overlay';

        let labsHtml = '<p style="color: #94a3b8;">No lab results extracted on active view. (Navigate to Lab Results tab to populate)</p>';
        if (labs.length > 0) {
            labsHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Test / Component</th>
                            <th>Result Value</th>
                            <th>Reference Range</th>
                            <th>Date / Draw Time</th>
                            <th>Specimen</th>
                            <th>Flag</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${labs.map(l => `
                            <tr>
                                <td style="font-weight: 600;">${l.testName}</td>
                                <td>${l.val}</td>
                                <td>${l.range}</td>
                                <td>${l.date} ${l.drawTime ? `<span style="color: #38bdf8; font-size: 10px;">(${l.drawTime})</span>` : ''}</td>
                                <td>${l.specimen || 'Specimen'}</td>
                                <td>${l.isCritical ? '<span class="bp-flag-critical">CRITICAL</span>' : (l.isAbnormal ? '<span class="bp-flag-abnormal">ABNORMAL</span>' : 'NORMAL')}</td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        let procsHtml = '<p style="color: #94a3b8;">No clinical procedures extracted on active view. (Navigate to Procedures tab to populate)</p>';
        if (procs.length > 0) {
            procsHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Procedure Name</th>
                            <th>Date Performed</th>
                            <th>Surgeon / Provider</th>
                            <th>Facility / Hospital</th>
                            <th>Body Site</th>
                            <th>Code</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${procs.map(p => `
                            <tr>
                                <td style="font-weight: 600;">${p.name}</td>
                                <td style="color: #38bdf8; font-weight: 600;">${p.date}</td>
                                <td>${p.provider}</td>
                                <td>${p.facility}</td>
                                <td>${p.bodySite}</td>
                                <td>${p.code}</td>
                                <td><span style="color: ${p.status === 'Completed' ? '#34d399' : '#38bdf8'}; font-weight: 600;">${p.status}</span></td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        let immsHtml = '<p style="color: #94a3b8;">No immunizations extracted on active view. (Navigate to Immunizations tab to populate)</p>';
        if (imms.length > 0) {
            immsHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Vaccine / Immunization</th>
                            <th>Date Administered</th>
                            <th>Age</th>
                            <th>Facility</th>
                            <th>Lot #</th>
                            <th>Manufacturer</th>
                            <th>Site & Route</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${imms.map(i => `
                            <tr>
                                <td style="font-weight: 600;">${i.vaccine}</td>
                                <td style="color: #38bdf8; font-weight: 600;">${i.date}</td>
                                <td>${i.age}</td>
                                <td>${i.facility}</td>
                                <td>${i.lotNumber || 'N/A'}</td>
                                <td>${i.manufacturer || 'N/A'}</td>
                                <td>${i.siteRoute || 'Standard'}</td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        let medsHtml = '<p style="color: #94a3b8;">No medications extracted on active view. (Navigate to Medications tab to populate)</p>';
        if (meds.length > 0) {
            medsHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Medication Name</th>
                            <th>Prescribed Date</th>
                            <th>Expiration Date</th>
                            <th>Instructions (Sig)</th>
                            <th>Prescriber</th>
                            <th>Refills</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${meds.map(m => `
                            <tr>
                                <td style="font-weight: 600;">${m.name}</td>
                                <td style="color: #38bdf8; font-weight: 600;">${m.prescribedDate || 'Recorded'}</td>
                                <td>${m.expirationDate || 'N/A'}</td>
                                <td>${m.instructions || 'As Directed'}</td>
                                <td>${m.prescriber || 'Enterprise Health System'}</td>
                                <td>${m.refills || '0'}</td>
                                <td><span style="color: ${m.status === 'Active' ? '#34d399' : '#94a3b8'}; font-weight: 600;">${m.status}</span></td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        let allergiesHtml = '<p style="color: #94a3b8;">No allergies recorded on active view.</p>';
        if (allergies.length > 0) {
            allergiesHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Allergen / Substance</th>
                            <th>Reaction</th>
                            <th>Severity</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${allergies.map(a => `
                            <tr>
                                <td style="font-weight: 600; color: #f87171;">${a.allergen}</td>
                                <td>${a.reaction}</td>
                                <td>${a.severity}</td>
                                <td>${a.status}</td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        let condsHtml = '<p style="color: #94a3b8;">No health conditions extracted on active view.</p>';
        if (conds.length > 0) {
            condsHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Health Issue / Diagnosis</th>
                            <th>Onset Date</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${conds.map(c => `
                            <tr>
                                <td style="font-weight: 600;">${c.condition}</td>
                                <td>${c.onsetDate}</td>
                                <td><span style="color: ${c.status.includes('Active') ? '#38bdf8' : '#94a3b8'}; font-weight: 600;">${c.status}</span></td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        let docsHtml = '<p style="color: #94a3b8;">No clinical notes/documents extracted on active view. (Navigate to Documents tab)</p>';
        if (docs.length > 0) {
            docsHtml = `
                <table class="bp-clinical-table">
                    <thead>
                        <tr>
                            <th>Document Title / Consultation</th>
                            <th>Date Updated</th>
                            <th>Format</th>
                        </tr>
                    </thead>
                    <tbody>
                        ${docs.map(d => `
                            <tr>
                                <td style="font-weight: 600;">${d.title}</td>
                                <td>${d.date}</td>
                                <td><span style="color: #38bdf8; font-weight: 700;">PDF</span></td>
                            </tr>
                        `).join('')}
                    </tbody>
                </table>
            `;
        }

        overlay.innerHTML = getTrustedHTML(`
            <div id="bp-mhs-modal">
                <div id="bp-mhs-modal-actions" class="bp-no-print" style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 20px; border-bottom: 1px solid #334155; padding: 18px 24px;">
                    <div>
                        <span style="font-size: 16px; font-weight: 700; color: #38bdf8;">🏥 Health Portal Consolidated Record</span>
                        <span style="font-size: 12px; color: #94a3b8; margin-left: 8px;">(${patName.replace(/_/g, ' ')})</span>
                    </div>
                    <div style="display: flex; gap: 8px; flex-wrap: wrap;">
                        <button class="bp-mhs-btn emerald" id="bp-btn-print-pdf">🖨️ Print / Save PDF</button>
                        <button class="bp-mhs-btn" id="bp-btn-batch-notes">📥 Batch Notes PDF</button>
                        <button class="bp-mhs-btn" id="bp-btn-csv-procs">🩺 Procedures CSV</button>
                        <button class="bp-mhs-btn" id="bp-btn-csv-meds">💊 Meds CSV</button>
                        <button class="bp-mhs-btn" id="bp-btn-csv-labs">📊 Labs CSV</button>
                        <button class="bp-mhs-btn" id="bp-btn-csv-imms">💉 Immunizations CSV</button>
                        <button class="bp-mhs-btn secondary" id="bp-btn-close-modal">✕ Close</button>
                    </div>
                </div>

                <div id="bp-mhs-modal-body">
                    <div style="border-bottom: 2px solid #38bdf8; padding-bottom: 10px; margin-bottom: 20px;">
                        <h2 style="margin: 0 0 6px 0;">Consolidated Clinical Summary: ${patName.replace(/_/g, ' ')}</h2>
                        <p style="margin: 0; font-size: 12px; color: #94a3b8;">Generated via BrowserParity Patient DX on ${now}</p>
                    </div>

                    <h3 style="color: #38bdf8; margin-top: 24px;">🩺 Surgical & Clinical Procedures</h3>
                    ${procsHtml}

                    <h3 style="color: #38bdf8; margin-top: 24px;">💊 Medications & Active Prescriptions</h3>
                    ${medsHtml}

                    <h3 style="color: #38bdf8; margin-top: 24px;">🔬 Laboratory & Diagnostic Results</h3>
                    ${labsHtml}

                    <h3 style="color: #38bdf8; margin-top: 24px;">💉 Immunization History</h3>
                    ${immsHtml}

                    <h3 style="color: #38bdf8; margin-top: 24px;">⚠️ Allergies & Adverse Reactions</h3>
                    ${allergiesHtml}

                    <h3 style="color: #38bdf8; margin-top: 24px;">📋 Health Issues & Chronic Conditions</h3>
                    ${condsHtml}

                    <h3 style="color: #38bdf8; margin-top: 24px;">📑 Clinical Notes & Consultation Documents</h3>
                    ${docsHtml}
                </div>
            </div>
        `);

        document.body.appendChild(overlay);

        // Bind Actions
        document.getElementById('bp-btn-print-pdf')?.addEventListener('click', () => { window.print(); });
        document.getElementById('bp-btn-batch-notes')?.addEventListener('click', batchDownloadClinicalNotes);
        document.getElementById('bp-btn-csv-procs')?.addEventListener('click', exportProceduresToCsv);
        document.getElementById('bp-btn-csv-meds')?.addEventListener('click', exportMedsToCsv);
        document.getElementById('bp-btn-csv-labs')?.addEventListener('click', exportLabsToCsv);
        document.getElementById('bp-btn-csv-imms')?.addEventListener('click', exportImmunizationsToCsv);
        document.getElementById('bp-btn-close-modal')?.addEventListener('click', () => { overlay.remove(); });
        overlay.addEventListener('click', (e) => { if (e.target === overlay) overlay.remove(); });

        document.addEventListener('keydown', function onEsc(e) {
            if (e.key === 'Escape') {
                const ov = document.getElementById('bp-mhs-modal-overlay');
                if (ov) ov.remove();
                document.removeEventListener('keydown', onEsc);
            }
        });
    }

    // ----------------------------------------------------
    // 6. HUMAN-LIKE INTERACTION & AUTONOMOUS MASTER DOSSIER ("COLLECT THEM ALL")
    // ----------------------------------------------------
    function simulateHumanClick(el, callback) {
        if (!el) {
            if (typeof callback === 'function') callback();
            return;
        }

        el.scrollIntoView({ behavior: 'smooth', block: 'center' });
        
        // Random micro-jitter
        const jitterX = Math.floor(Math.random() * 15) + 5;
        const jitterY = Math.floor(Math.random() * 10) + 5;
        el.dispatchEvent(new MouseEvent('mousemove', { bubbles: true, clientX: jitterX, clientY: jitterY }));
        
        setTimeout(() => {
            el.dispatchEvent(new MouseEvent('mouseenter', { bubbles: true }));
            el.dispatchEvent(new MouseEvent('mousedown', { bubbles: true, button: 0 }));
            setTimeout(() => {
                el.dispatchEvent(new MouseEvent('mouseup', { bubbles: true, button: 0 }));
                el.click();
                
                // Human-like post-click pause (1.4s - 2.8s)
                const humanDelay = 1400 + Math.floor(Math.random() * 1400);
                setTimeout(() => {
                    if (typeof callback === 'function') callback();
                }, humanDelay);
            }, 80 + Math.floor(Math.random() * 80));
        }, 100 + Math.floor(Math.random() * 120));
    }

    function getActiveClinicalDomain() {
        const url = window.location.href.toLowerCase();
        const title = (document.title || '').toLowerCase();
        
        // 1. Strict URL & Page Title matching
        if (url.includes('medication') || title.includes('medication') || !!document.querySelector('a.active[href*="medication"], [aria-selected="true"][aria-label*="Medication" i]')) {
            return { type: 'meds', label: '💊 Medications', actionName: 'Export Meds CSV', handler: exportMedsToCsv };
        } else if (url.includes('result') || url.includes('lab') || url.includes('measurement') || title.includes('result') || title.includes('measurement') || !!document.querySelector('a.active[href*="result"], [aria-selected="true"]#results-tab')) {
            return { type: 'labs', label: '📊 Labs & Vitals', actionName: 'Export Labs CSV', handler: exportLabsToCsv };
        } else if (url.includes('procedure') || title.includes('procedure') || !!document.querySelector('a.active[href*="procedure"], [aria-selected="true"][aria-label*="Procedure" i]')) {
            return { type: 'procedures', label: '🩺 Procedures', actionName: 'Export Procedures CSV', handler: exportProceduresToCsv };
        } else if (url.includes('immuniz') || title.includes('immuniz') || !!document.querySelector('a.active[href*="immuniz"], [aria-selected="true"][aria-label*="Immuniz" i]')) {
            return { type: 'imms', label: '💉 Immunizations', actionName: 'Export Immunizations CSV', handler: exportImmunizationsToCsv };
        } else if (url.includes('allerg') || title.includes('allerg') || !!document.querySelector('a.active[href*="allerg"], [aria-selected="true"][aria-label*="Allerg" i]')) {
            return { type: 'allergies', label: '⚠️ Allergies', actionName: 'Export Allergies CSV', handler: exportAllergiesToCsv };
        } else if (url.includes('condition') || url.includes('issue') || title.includes('condition') || !!document.querySelector('a.active[href*="condition"], [aria-selected="true"][aria-label*="Health Issue" i]')) {
            return { type: 'conditions', label: '📋 Health Issues', actionName: 'Export Conditions CSV', handler: exportConditionsToCsv };
        } else if (url.includes('document') || title.includes('clinical note') || !!document.querySelector('a.active[href*="document"], [aria-selected="true"][aria-label*="Clinical Notes" i]')) {
            return { type: 'docs', label: '📄 Clinical Notes', actionName: 'Batch Notes PDF', handler: batchDownloadClinicalNotes };
        } else if (url.includes('messaging') || url.includes('inbox') || title.includes('messaging') || !!document.querySelector('a.active[href*="messaging"]')) {
            return { type: 'messages', label: '💬 Secure Messages', actionName: 'Export Messages', handler: openConsolidatedDossier };
        } else if (url.includes('appointment') || title.includes('appointment') || !!document.querySelector('a.active[href*="appointment"]')) {
            return { type: 'appointments', label: '📅 Appointments', actionName: 'Export Appointments', handler: openConsolidatedDossier };
        } else if (url.includes('visit') || title.includes('visit summary') || !!document.querySelector('a.active[href*="visit"]')) {
            return { type: 'summaries', label: '📑 Visit Summaries', actionName: 'Download CCD', handler: openConsolidatedDossier };
        }
        return { type: 'all', label: '🏥 Clinical Record', actionName: 'Consolidated Dossier', handler: openConsolidatedDossier };
    }

    function collectAllClinicalRecords() {
        showToast('🚀 Starting Full Autonomous Clinical Dossier Tour (Human-Like Pace)...');
        
        const tabs = [
            { label: 'Health Record / Labs', selector: '#application-layout-tab-1, a[href*="results"], [aria-label*="Results"]' },
            { label: 'Medications', selector: 'a[href*="medications"], [aria-label*="Medication"]' },
            { label: 'Procedures', selector: 'a[href*="procedure"], [aria-label*="Procedure"]' },
            { label: 'Immunizations', selector: 'a[href*="immunizations"], [aria-label*="Immunization"]' },
            { label: 'Allergies & Conditions', selector: 'a[href*="allerg"], a[href*="condition"], [aria-label*="Health Issue"]' },
            { label: 'Documents & Notes', selector: 'a[href*="documents"], [aria-label*="Clinical Notes"]' }
        ];

        let tabIdx = 0;

        function processNextTab() {
            if (tabIdx >= tabs.length) {
                showToast('🎉 Full Autonomous Collection Complete! Assembling Master Health Dossier...');
                setTimeout(openConsolidatedDossier, 1200);
                return;
            }

            const current = tabs[tabIdx];
            showToast(`🧭 [${tabIdx + 1}/${tabs.length}] Navigating to ${current.label}...`);

            const tabEl = document.querySelector(current.selector);
            if (tabEl) {
                simulateHumanClick(tabEl, () => {
                    // If on tabs with scrollable history, unroll infinite scroll
                    if (current.label.includes('Labs') || current.label.includes('Medications') || current.label.includes('Procedures')) {
                        unrollInfiniteScroll(() => {
                            tabIdx++;
                            setTimeout(processNextTab, 1800);
                        });
                    } else {
                        tabIdx++;
                        setTimeout(processNextTab, 2000);
                    }
                });
            } else {
                tabIdx++;
                setTimeout(processNextTab, 1000);
            }
        }

        processNextTab();
    }

    // Expose Global Bridges for Remote Control Deck & Test Automation
    window.__bp_collect_all_clinical_records = collectAllClinicalRecords;
    window.__bp_unroll_infinite_scroll = () => unrollInfiniteScroll();
    window.__bp_crawl_paginated_documents = () => batchDownloadClinicalNotes();
    window.__bp_extractors = {
        extractMedicationsData,
        extractLabsData,
        extractProceduresData,
        extractImmunizationsData,
        extractAllergiesData,
        extractConditionsData,
        extractClinicalDocuments,
        getActiveClinicalDomain
    };

    // ----------------------------------------------------
    // 7. REACTIVE ARIA LIVE LISTENER
    // ----------------------------------------------------
    function attachAriaLiveObserver() {
        getContextDocuments().forEach(doc => {
            if (doc._bpLiveObsAttached) return;
            doc._bpLiveObsAttached = true;

            const obs = new MutationObserver(mutations => {
                for (const m of mutations) {
                    if (m.addedNodes.length > 0) {
                        m.addedNodes.forEach(node => {
                            if (node.nodeType === 1) {
                                const text = node.innerText || '';
                                if (/Results Loaded|Medications Loaded|Conditions Loaded|More results loaded|More vitals loaded/i.test(text)) {
                                    showToast(`⚡ ${text.trim()}`);
                                }
                            }
                        });
                    }
                }
            });
            obs.observe(doc.body || doc.documentElement, { childList: true, subtree: true });
        });
    }

    // ----------------------------------------------------
    // 8. FLOATING QUICK ACTION BADGE & SHORTCUTS
    // ----------------------------------------------------
    function injectBadge() {
        ensureStylesInjected();
        if (window.top !== window.self || !document.body) return;

        const domain = getActiveClinicalDomain();
        let badge = document.getElementById('bp-mhs-badge');
        if (!badge) {
            badge = document.createElement('div');
            badge.id = 'bp-mhs-badge';
            badge.style.cssText = 'position: fixed !important; bottom: 16px !important; right: 16px !important; z-index: 2147483647 !important; display: flex !important; align-items: center !important; gap: 8px !important; padding: 6px 14px !important; background: rgba(15, 23, 42, 0.94) !important; color: #38bdf8 !important; border: 1px solid rgba(56, 189, 248, 0.5) !important; border-radius: 9999px !important; font-family: Segoe UI, system-ui, sans-serif !important; font-size: 11px !important; font-weight: 700 !important; cursor: pointer !important; box-shadow: 0 4px 16px rgba(0, 0, 0, 0.5) !important; backdrop-filter: blur(10px) !important; user-select: none !important;';
            document.body.appendChild(badge);
        }

        badge.title = `Active Context: ${domain.label}. Click for options or Alt+M for Dossier.`;
        badge.innerHTML = `
            <span>${domain.label}</span>
            <span style="background: #0284c7; color: #fff; font-size: 9px; padding: 2px 6px; border-radius: 6px; font-weight: normal;">Collect All</span>
        `;
        
        badge.onclick = (e) => {
            // If user clicks "Collect All" pill directly
            if (e.target.innerText.includes('Collect All')) {
                collectAllClinicalRecords();
            } else {
                openConsolidatedDossier();
            }
        };

        attachAriaLiveObserver();
    }

    // Keyboard shortcuts: Alt + M (Dossier), Alt + P (Procedures), Alt + L (Labs CSV), Alt + I (Immunizations CSV), Alt + N (Clinical Notes), Alt + C (Collect All)
    window.addEventListener('keydown', (e) => {
        if (e.altKey && (e.key === 'm' || e.key === 'M')) {
            e.preventDefault();
            openConsolidatedDossier();
        }
        if (e.altKey && (e.key === 'p' || e.key === 'P')) {
            e.preventDefault();
            exportProceduresToCsv();
        }
        if (e.altKey && (e.key === 'c' || e.key === 'C')) {
            e.preventDefault();
            collectAllClinicalRecords();
        }
        if (e.altKey && (e.key === 'l' || e.key === 'L')) {
            e.preventDefault();
            exportLabsToCsv();
        }
        if (e.altKey && (e.key === 'i' || e.key === 'I')) {
            e.preventDefault();
            exportImmunizationsToCsv();
        }
        if (e.altKey && (e.key === 'n' || e.key === 'N')) {
            e.preventDefault();
            batchDownloadClinicalNotes();
        }
    });

    setInterval(injectBadge, 2000);
    injectBadge();
})();